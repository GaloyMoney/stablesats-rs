## This file documents the defaults:

# pubsub:
#   host: localhost

# price_server:
  # enabled: true
  # server:
  #   listen_port: 3325
  # fees:
  #   base_fee_rate: 0.0005
  #   immediate_fee_rate: 0.0005
  #   delayed_fee_rate: 0.0007

# okex_price_feed:
  # enabled: true
  # config:
  #   url: "wss://ws.okx.com:8443/ws/v5/public"

# tracing:
#   host: "localhost"
#   port: 6831
#   service_name: "stablesats-dev"
