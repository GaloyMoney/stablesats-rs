## This file documents the defaults:

# pubsub:
#   host: localhost

# price_server:
  # enabled: true
  # config:
  #   listen_port: 3325

# okex_price_feed:
  # enabled: true
  # config:
  #   url: "wss://ws.okx.com:8443/ws/v5/public"
